Build a Discord bot using discord.py that supports a collectible trading card system themed around rockets. This is Phase 1 of a multi-phase rollout. Deliver production-ready code, database migrations, and modular command logic. Use PostgreSQL via asyncpg. The bot uses `!` as its command prefix.

---

🎯 Scope

Implement the following features:

✅ Core Systems

• `!drop`: Users can claim 2 randomized cards every 8 hours. Enforce cooldown via `last_drop_ts`.
• `!mycards`: Lists owned cards, sorted by rarity (ascending: Common → Mythic) then alphabetically.
• `!cardinfo [name or ID]`: Shows card details including image, rarity, description, and stats.
• `!addcard [rarity] [name_with_underscores] [description_with_underscores_for_newlines]`: Admin-only. Accepts image attachment and stores it with the card.
• Assign a UUID to each dropped card instance.
• Track credits per user for future microtransactions.
• Validate rarity against this hierarchy: Common, Uncommon, Exceptional, Rare, Epic, Legendary, Mythic.


🧱 Database Schema

Create the following tables:

-- Player profile and cooldown tracking
CREATE TABLE players (
  user_id BIGINT PRIMARY KEY,
  credits INT DEFAULT 0,
  last_drop_ts TIMESTAMP WITH TIME ZONE
);

-- Master card definitions
CREATE TABLE cards (
  card_id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  rarity TEXT NOT NULL,
  description TEXT,
  image_url TEXT,
  stats JSONB DEFAULT '{}'::jsonb,
  created_by BIGINT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT now()
);

-- Player-owned card instances
CREATE TABLE user_cards (
  instance_id UUID PRIMARY KEY,
  user_id BIGINT NOT NULL,
  card_id INT REFERENCES cards(card_id),
  acquired_at TIMESTAMP WITH TIME ZONE DEFAULT now(),
  source TEXT
);

🧠 Sorting Logic

• `!mycards` sorts by rarity using the defined hierarchy, then alphabetically by name.
• Display card name, rarity, and image (if available).


🔐 Permissions

• Admin commands restricted to bot owner or users in a configurable `admin_ids` list.
• Validate image attachment in `!addcard`; reject if missing.


---

🧱 Foundation for Future Features

Implement scaffolding for:

• Recycling cards: Add a placeholder command `!recycle [instance_id]` that validates ownership and deletes the card. Do not credit user yet.
• Trading system: Add a placeholder table `pending_trades` with sender_id, receiver_id, offered_instance_id, requested_instance_id, status.
• Microtransactions: Store credits in `players` table. Add placeholder command `!buycredits` that simulates adding credits (no Stripe integration yet).
• Card activities: Add placeholder command `!launch [instance_id]` that validates card ownership and logs the attempt. No gameplay logic yet.
• Image updates: Add admin-only command `!updateimage [card_id]` that accepts a new image attachment and updates the card’s image_url.


---

🧪 Testing & Validation

• Validate rarity input against allowed values.
• Enforce 8-hour cooldown for `!drop`.
• Ensure each dropped card gets a unique UUID.
• Confirm image URL is stored correctly from attachment.
• Confirm sorting logic in `!mycards` matches rarity + name.
• Confirm admin-only commands are restricted.


---

🗂️ File-Level Tasks

• `db/migrations/0001_cardbot.sql`: Create all tables above
• `cogs/cards.py`: Implement all commands
• `utils/card_helpers.py`: Rarity sorting, cooldown checks, image validation
• `tests/test_cards.py`: Unit tests for drop logic, sorting, and admin commands